{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Program",
			"preLaunchTask": "Debug",
			"request": "launch",
			"type": "coreclr",
			"program": "${workspaceFolder}/lib/Belin.Which.dll",
			"args": ["${input:command}"]
		}
	],
	"inputs": [
		{
			"description": "The name of the executable to find.",
			"id": "command",
			"type": "promptString"
		}
	]
}
